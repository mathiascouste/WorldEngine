Pour générer un monde :
1 - Créer une instance World en précisant sa taille (nombre d'Area de haut et de côté)
2 - Créer une instance WorldGenerator en lui passant le monde précédemment créé en paramètre
3 - Appeler la méthode "generateMap()" de l'instance de WorldGenerator créée.

Voilà le monde est créé.

Pour lancer la simulation :
1 - Appeler la méthode "start()" de l'instance World.
